#!/bin/sh

plugin_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)

if [ -x "$plugin_dir/bin/dank-soulver-core" ]; then
    exec "$plugin_dir/bin/dank-soulver-core" "$@"
fi

exec dank-soulver-core "$@"
